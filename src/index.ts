// import { Permutation } from './combinatorics';

export * from './utils';
export * from './geometry';
// const P = new Permutation([4, 1, 7, 3, 6, 5, 2, 8]);

// console.log(P);
// console.log(P.transpositions.reduce((acc, next) => acc.compose(next)));
